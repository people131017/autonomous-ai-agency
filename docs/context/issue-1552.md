# Issue #1552: Routine backlog — 2026-W39

_Generated: 2026-09-21_

## Context Plan — Issue #1552: Routine backlog — 2026-W39

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Source Grounding

| | |
|---|---|
| Source URL | https://code.claude.com/docs/en/changelog |
| Fetch status | ⚠️ **NOT FETCHED** — the plan below is unverified against the source |
| Generated by | NVIDIA NIM · `nvidia/nemotron-3-nano-omni-30b-a3b-reasoning` |
| Repair rounds | 1 |
| Rulebook | `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md` |

---

## What the source actually is

The GitHub issue body describes two concrete bug fixes: (1) implement extraction of X-Session-Id and X-Claude-Code-Session-Id headers and propagate them as Langfuse sessionId tags across all chat request paths, correcting a changelog entry that claimed this was already done but git grep shows zero matches; (2) modify the Bedrock provider to omit explicit empty credential arguments so boto3 can use its default credential chain when static keys are unset. No external URL content was fetched; this summary is drawn solely from the issue body.

---

## Decision

✅ **ADOPT** — build largely as described in the source.

Both items are necessary bug fixes that correct false changelog claims and enable proper credential handling; they affect only non-risky modules, are additive, and preserve existing behavior when keys are set, satisfying all rules.

---

## Implementation Prompt

Implement a minimal, additive fix that (1) extracts the X-Session-Id and X-Claude-Code-Session-Id request headers (case-insensitive, preferring X-Claude-Code-Session-Id when both are present) in the chat request handling path, passes the value as `session_id` to `langfuse_obs.emit_chat_observation`, and adds the corresponding `session:<id>` tag. This touches `proxy.py` (or the equivalent chat handler) and `langfuse_obs.py`; ensure the extraction logic works for both streaming and non-streaming request flows and that no user-visible behavior changes when the headers are absent. (2) modify the Bedrock provider logic in `packages/ai/router.py` (`_post_bedrock_converse`) to conditionally include `aws_access_key_id` and `aws_secret_access_key` arguments only when the corresponding environment variables are set; otherwise call `boto3.client` without those kwargs so the default credential chain is used. Preserve existing behavior when static keys are configured. Add unit tests that mock `langfuse_obs.emit_chat_observation` to verify `session_id` and tag arguments are correctly passed for streaming and non-streaming paths, and that the Bedrock provider omits credential kwargs when env vars are unset while including them unchanged when set. Ensure changelog parity by updating `CHANGELOG.md` and `docs/changelog.md` to reflect the implemented header handling.

---

## TODO List

- [ ] **[S]** Add header extraction logic in the chat request handling function (e.g., `handle_chat` in `proxy.py`) to read X-Session-Id and X-Claude-Code-Session-Id, resolve precedence, and pass `session_id` to `langfuse_obs.emit_chat_observation`. — `proxy.py`
      <br/>_Done when:_ emit_chat_observation receives a non-None `session_id` argument and a `session:<id>` tag is added to the trace.
- [ ] **[S]** Extend Langfuse tagging to include X-Claude-Code-Agent-Type and X-Claude-Code-Request-Class headers when present, mirroring the session tag pattern. — `langfuse_obs.py`
      <br/>_Done when:_ Trace metadata contains additional tags/keys for the two new headers, verified by a unit test.
- [ ] **[S]** Update Bedrock provider credential handling in `packages/ai/router.py` (`_post_bedrock_converse`) to omit `aws_access_key_id` and `aws_secret_access_key` kwargs when the corresponding env vars are empty, allowing boto3 to use its default credential chain. — `packages/ai/router.py`
      <br/>_Done when:_ boto3.client is called without the two credential kwargs when env vars are unset, and with them unchanged when set, confirmed by a mocked boto3 client test.
- [ ] **[M]** Add unit tests covering header extraction and Langfuse tagging for both streaming and non-streaming paths, and Bedrock provider credential omission vs inclusion. — `tests/test_bedrock_provider.py`
      <br/>_Done when:_ All new tests pass, asserting correct `session_id` and tag arguments and correct omission of credential kwargs.
- [ ] **[S]** Update `CHANGELOG.md` and `docs/changelog.md` to reflect the implemented header propagation and Bedrock credential fallback, ensuring byte-identical parity. — `CHANGELOG.md, docs/changelog.md`
      <br/>_Done when:_ Both changelog files contain matching entries describing the new header handling and Bedrock credential behavior.

---

## Relevant Files to Read First

- `proxy.py`
- `langfuse_obs.py`
- `packages/ai/router.py`
- `tests/test_bedrock_provider.py`
- `CHANGELOG.md`
- `docs/changelog.md`

---

## Risk Flags

- ⚠️ `proxy.py`
- ⚠️ `langfuse_obs.py`
- ⚠️ `packages/ai/router.py`

---

## Architectural Notes

The changes are purely additive and non-breaking; they align with rule 34 changelog parity, do not modify any risky modules, and preserve existing user-visible behavior when the new headers are absent. The Bedrock provider modification respects the existing static-key workflow and only alters behavior when credentials are unset. Assuming the chat request handling lives in `proxy.py` (unverified) — if it's in `chat_handlers.py` or `handlers/anthropic_compat.py` instead, the file target for step 1 changes accordingly.

---

## Quality Gate

⚠️ **1 unmet rule(s)** from `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md`. This plan was shipped anyway so a reviewer can see exactly where it is thin — treat the items below as unresolved before implementing.

- **R1** — the linked source was not retrieved, so every claim about it is unverified; the document must be reviewed before implementation

---

*Closes #1552*

