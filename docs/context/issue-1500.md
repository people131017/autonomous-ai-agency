# Issue #1500: quick-note:https://x.com/sairahul1/status/2098699529487028643?s=46

_Generated: 2026-09-14_

## Context Plan — Issue #1500: quick-note:https://x.com/sairahul1/status/2098699529487028643?s=46

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Source Grounding

| | |
|---|---|
| Source URL | https://x.com/sairahul1/status/2098699529487028643?s=46 |
| Fetch status | ✅ fetched — 6,000 chars of content |
| Generated by | NVIDIA NIM · `nvidia/nemotron-3-nano-omni-30b-a3b-reasoning` |
| Repair rounds | 1 |
| Rulebook | `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md` |

---

## What the source actually is

The article explains how to build a $50/month AI workforce using GPT‑6 Astra as the orchestrator and DeepSeek V4.1 Flash as a cheap subagent, by installing codex‑router, enabling the DeepSeek provider, selecting the flash model, and configuring Astra to explicitly delegate tasks to Flash.

---

## Decision

✅ **ADOPT** — build largely as described in the source.

The repository currently lacks DeepSeek integration and explicit subagent routing for Flash; adding a provider and configuring Astra to delegate to Flash fulfills the article's goal while staying within the existing routing architecture.

---

## Implementation Prompt

The linked guide describes a low‑cost AI workforce where GPT‑6 Astra acts as the brain and DeepSeek V4.1 Flash performs the heavy lifting at a fraction of the cost. To realize this in autonomous‑ai‑agency, the first step is to implement a DeepSeek provider that conforms to the existing provider interface (generate, chat, stream, health, cost, limits) and registers the `deepseek/deepseek-v4-flash` model in the model registry with a low cost tier. Next, configuration options must be added to enable Flash as a delegated subagent, including a boolean flag `use_flash_subagent`, a `reasoning_effort` setting (default "low"), and an `experimental_mode` toggle. The Astra system prompt in `agents/profiles.py` should be updated so that when `use_flash_subagent` is true, Astra explicitly creates subtasks and routes them through the codex‑router to the Flash model, rather than executing everything itself. This requires modifying the prompt file `agents/profiles.py` to embed instructions that trigger delegation based on the config flag. After the provider and configuration are in place, a unit test should be added to `tests/test_model_router.py` that verifies a delegated task resolves to the Flash model. Finally, documentation must be updated in `docs/configuration-reference.md` to describe the new provider and its usage, and any relevant API surface changes should be noted in `docs/api-surfaces.md`. All changes must respect the existing routing invariants: the router never raises, returns a decision with a resolved model, and fallbacks work as before. The implementation should be fully asynchronous, use the shared axios client for HTTP calls, and avoid any direct SDK usage. By following these steps, the system will support the $50/month workflow described in the article while preserving user‑visible behavior and adhering to the project's wiring and security rules.

---

## TODO List

- [ ] **[M]** Create DeepSeek provider implementation file `packages/ai/providers/deepseek.py` with async `generate`, `chat`, `stream`, `health`, `cost`, and `limits` methods that call the DeepSeek API. — `packages/ai/providers/deepseek.py (new)`
      <br/>_Done when:_ File `packages/ai/providers/deepseek.py` exists and contains the required async methods with proper type hints.
- [ ] **[M]** Add DeepSeek V4.1 Flash model entry to `config/models.yaml` with `strengths` and `cost_tier` fields and ensure it is registered in `MODEL_REGISTRY` (e.g., via `packages/ai/router.py`). — `config/models.yaml`
      <br/>_Done when:_ `config/models.yaml` contains a `deepseek/deepseek-v4-flash` entry with appropriate `strengths` and `cost_tier`, and the model appears in the runtime registry.
- [ ] **[M]** Add configuration keys `use_flash_subagent`, `reasoning_effort`, and `experimental_mode` to `packages/ai/brain_config.py` and ensure they are loaded from environment or config files. — `packages/ai/brain_config.py`
      <br/>_Done when:_ `packages/ai/brain_config.py` contains the three new boolean/string configuration options and they are accessible via the BrainConfig instance.
- [ ] **[M]** Modify Astra's system prompt in `agents/profiles.py` to include explicit instructions that when `use_flash_subagent` is true, Astra should spawn subtasks and route them to DeepSeek Flash via the codex‑router. — `agents/profiles.py`
      <br/>_Done when:_ The system prompt in `agents/profiles.py` contains clear delegation language referencing DeepSeek Flash and the routing mechanism.
- [ ] **[M]** Add a regression test in `tests/test_model_router.py` that creates a task, forces delegation to Flash, and asserts that the resolved model is `deepseek/deepseek-v4-flash`. — `tests/test_model_router.py`
      <br/>_Done when:_ The test passes, confirming that a delegated task uses the Flash model as expected.
- [ ] **[S]** Update documentation: add a section for the DeepSeek provider in `docs/configuration-reference.md` and note usage in `docs/api-surfaces.md` if new endpoints are introduced. — `['docs/configuration-reference.md', 'docs/api-surfaces.md']`
      <br/>_Done when:_ `docs/configuration-reference.md` contains a description of the DeepSeek provider and its configuration options, and `docs/api-surfaces.md` references any new API behavior.

---

## Relevant Files to Read First

- `packages/ai/providers/deepseek.py (new)`
- `config/models.yaml`
- `packages/ai/brain_config.py`
- `agents/profiles.py`
- `tests/test_model_router.py`
- `docs/configuration-reference.md`
- `docs/api-surfaces.md`

---

## Risk Flags

_No risky modules flagged._

---

## Architectural Notes

All provider calls must go through `packages/ai/router.py`; the new DeepSeek provider must respect token caching and cost calculations. Ensure the system prompt change does not alter user‑visible behavior unless explicitly requested. Run `graphify update .` after modifications and commit the regenerated `graphify-out/GRAPH_REPORT.md`.

---

## Quality Gate

✅ Passed every machine-checked rule in `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md`.

---

*Closes #1500*

